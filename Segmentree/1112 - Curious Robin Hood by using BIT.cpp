#include<bits/stdc++.h>
using namespace std;
const int MxN = 500005;
int n, q;
int ar[]
int query()

int main()
{
    int tc ;
    cin>>tc;
    int cs = 0, y,c , x, ;
    while(tc--)
    {
        cs++;
        cin>>n>>q;
        for(int i = 1; i <=n ; i++ )
            scanf("%d",&ar[i]);
        for(int i = 0 ; i < q ; i++)
        {
            scanf("%d",&c);
            if(c==1)
            {
                scanf("%d",&x);
            }
            else if(c==2)
            {
                scanf("%d%d",&x, %y);

            }
            else
            {
                scanf("%d%d",&x ,&y);
            }
        }
    }
    return 0;
}
